import { Text } from '@src/components';
import React from 'react';
import { View } from 'react-native';

export const MarkerList = (): React.ReactElement => {
  console.log('list');

  return (
    <View>
      <Text variant="h1">Marker list</Text>
    </View>
  );
};
